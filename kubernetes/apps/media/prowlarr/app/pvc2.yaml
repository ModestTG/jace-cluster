apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: prowlarr-config-new
  namespace: media
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  dataSource:
    kind: PersistentVolumeClaim
    name: prowlarr-config
  storageClassName: longhorn
